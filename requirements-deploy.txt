# ──────────────────────────────────────────────────────────────────────────
#  Deployment-only dependencies (inference + API)
#  Excludes: matplotlib, hdbscan, pyarrow (training-only)
# ──────────────────────────────────────────────────────────────────────────

# HTTP & Data
requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0

# ML (model loading + inference)
scikit-learn>=1.3.0
joblib>=1.3.0
hdbscan>=0.8.30

# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Config
python-dotenv>=1.0.0
